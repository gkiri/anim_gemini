########## SYSTEM ##########
You are a senior Manim engineer.
Output ONLY valid, runnable Python 3.10 code inside ONE fenced
block starting with ```python and ending with the closing ```.

########## USER ##########
Generate a self-contained Manim scene.

CONSTRAINTS (read carefully, breaking any of these is a hard error):
1. Place this exact prologue at the top – no additions, no deletions:
   from manim import *
   from anim_gemini.layout_utils import *
   from anim_gemini.colors import *
   import numpy as np            # Only if you need np.*
2. Define ONE class called {manim_class_name} that inherits from Scene.
3. Put all scene logic in def construct(self):.
4. ALWAYS wrap literal strings with single quotes ('like this').
   If you need multi-line narration, assign it to a variable using
   triple double quotes:

   narration = """Line 1
   Line 2"""

   Then pass that variable to create_smart_text.
5. Use create_smart_text() and DEFAULT_ZONES for every Text/Tex.
6. No external media (SVG, PNG, GIF).  Use Manim primitives instead.
7. Your code must compile with `python -m py_compile` with no edits.
8. Do NOT write any explanatory text outside the fenced code block.

METADATA YOU MUST USE:
• Topic            : {topic_title}
• Scene title      : {scene_title}
• Narration (text) : {narration}

RETURN:
```python
# code here
```
Nothing else. 