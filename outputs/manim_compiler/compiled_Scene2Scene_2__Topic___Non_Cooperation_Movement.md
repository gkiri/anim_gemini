# Compilation Log for: Scene2Scene_2__Topic___Non_Cooperation_Movement

## Source SDL Scene:

```json
{
    "scene_number": 2,
    "title": "Scene 2: Topic - Non-Cooperation Movement",
    "narration": "This is the narration for scene 2 concerning Non-Cooperation Movement. More details would be generated by an AI.",
    "mood_color_profile": "DEFAULT",
    "total_scene_duration_hint": 8.0,
    "sdl_elements": [
        {
            "id": "title_text",
            "type": "Text",
            "content": "Scene 2: Topic - Non-Cooperation Movement",
            "purpose": "title",
            "position_tag": "TOP_CENTER",
            "extra": {
                "font_size": 60,
                "color": "YELLOW"
            }
        }
    ],
    "sdl_animations": [
        {
            "action": "Write",
            "target": "title_text",
            "duration": 2.0,
            "delay": 0.0
        }
    ]
}
```

## Generated Manim Python Code (Scene2Scene_2__Topic___Non_Cooperation_Movement.py):

```python
import sys
import os
from pathlib import Path

# Path manipulation to allow importing anim_gemini.project_drishti
# Assumes the script is in <workspace_root>/anim_gemini/outputs/generated_content/manim_scripts/
# And project_drishti is in <workspace_root>/anim_gemini/project_drishti/
# We add <workspace_root> to sys.path.
script_dir = Path(__file__).resolve().parent
# To reach <workspace_root> from e.g. /root/manim_gemini/anim_gemini/outputs/generated_content/manim_scripts/
# we go up 4 levels: manim_scripts -> generated_content -> outputs -> anim_gemini -> <workspace_root>
project_root_for_import = script_dir.parents[4] # <workspace_root>
if str(project_root_for_import) not in sys.path:
    sys.path.insert(0, str(project_root_for_import))

from manim import *
from anim_gemini.layout_utils import *
import numpy as np

class Scene2Scene_2__Topic___Non_Cooperation_Movement(Scene):
    def construct(self):
        # Scene: Scene 2: Topic - Non-Cooperation Movement
        # Narration: This is the narration for scene 2 concerning Non-Cooperation Movement. More details would be generated by an AI.
        # Mood/Color: DEFAULT

        title_text = create_smart_text("Scene 2: Topic - Non-Cooperation Movement",
                                                  zone_name="TITLE_AREA",
                                                  target_zone_width=11.52,
                                                  font_size=48,
                                                  max_font_size=60,
                                                  min_font_size=16)

        self.play(Write(title_text, duration=2.0))
        self.wait(max(1, 8.0 - 2.00))
```
